# --- deploy tooling + secrets — never commit (live SSH / DB / JWT credentials) ---
deploy.py
deploy_config.json
credential.md

# --- dependencies / build output (utech-repo has its own nested .gitignores too) ---
node_modules/
**/node_modules/
utech-repo/backend/public/
utech-repo/frontend/dist/
utech-repo/**/uploads/
dist/
build/
.next/

# --- env files ---
.env
.env.*
!.env.example

# --- logs / runtime ---
*.log
*.pid
db/*.db

# --- editor / OS ---
.DS_Store
.vscode/
.idea/
.qodo/
.claude/
