   186→  Confirmed via API: GET /api/invoices shows subtotal=Σ(lines)=15000 and amountPaid=Σ(payments) for every invoice. Seed change does not require backend restart (seed not loaded by the running server); script left at /home/z/tmp/fix-invoice-totals.js.
   187→- C) Jobcards labeling: AppLayout.jsx nav item /jobcards "Projects"→"Jobcards" (icon kept); JobcardsPage PageHeader "Projects"→"Jobcards", "New Project"→"New Jobcard", error/delete-confirm/toast strings updated ("Project No" column kept as instructed).
   188→- D) Inactive parties: PartiesPage name cell now renders a muted slate "Inactive" Badge + dims the name for isActive=false; added All parties/Active/Inactive select. party.controller.js list has no ?isActive support, so the filter is client-side over the loaded rows (visibleRows) per instructions.
   189→- E) 404: App.jsx catch-all `<Navigate to="/">` replaced by an inline NotFound component (centered card: big "404", "Page not found", explanation, "Back to Dashboard" primary button linking to /); URL no longer silently rewrites.
   190→- Verification: `bun run lint` 0 errors (71 pre-existing warnings, none new); `bunx vite build` OK; dist re-copied to backend/public; `node --check backend/prisma/seed.js` + `npx eslint prisma/seed.js` clean; backend `npm run lint` clean; served HTML references the new bundle and the asset returns 200.
   191→
   192→Stage Summary:
   193→- Commit 4692b66 "fix(ux): amount-in-words on invoices, Jobcards nav label, inactive party badge, 404 page, seed totals reconciliation" on master, parent d050c61. NOTE: the planned HEAD-message amend (typo "security, security") was NOT needed — d050c61's subject already reads exactly "fix: security, stock integrity, UX hardening — full audit remediation" (verified byte-level with od -c), so it was left untouched to avoid a pointless hash rewrite.
   194→- Deployed asset hash: index-MTVP5XtM.js (curl of / shows it; GET /assets/index-MTVP5XtM.js → 200).
   195→- Browser spot-checks (agent-browser, admin@utech.local): /invoices/5 → totals Subtotal ₹15,000.00 / CGST+SGST ₹1,350.00 / Total ₹17,700.00 / Paid ₹500.00 / Balance ₹17,200.00 + "AMOUNT IN WORDS: Seventeen Thousand Seven Hundred Rupees Only"; /jobcards → sidebar item and page heading both "Jobcards"; /parties → "Browser Test Party" row shows "Inactive" badge, filter Inactive shows only P-0011, Active hides it, All shows 11 rows; /some-bogus-url → stays on URL and shows the 404 card, "Back to Dashboard" returns to /. Zero page errors; console only has the benign recharts width/height warnings seen in prior QA.
   196→---
   197→Task ID: 5-a
   198→Agent: preview-restore-agent
   199→Task: Restore ERP preview login + verify credentials
   200→
   201→Work Log:
   202→- Status check on arrival: ports 3000 (next-server pid 1100) + 4000 (backend pid 1167) UP, 3306 DOWN. erp-services.log showed watchdog loop: "starting mariadbd … spawn /home/z/mariadb/root/usr/sbin/mariadbd ENOENT". Backend backend.log confirmed login 500 = Prisma "Can't reach database server at 127.0.0.1:3306".
   203→- ROOT CAUSE: the ENTIRE /home/z/mariadb tree (binaries, libs, AND datadir with all data) had been wiped from disk before this session began (env-level reset — nothing to preserve; task's "never delete datadir" respected: I deleted nothing). Watchdog was healthy, just had no binary to spawn.
   204→- Rebuilt the exact layout instrumentation.ts expects (no Next.js files touched): Debian 13 (trixie) repos carry the identical version 1:11.8.6-0+deb13u1 — `apt-get download mariadb-server-core mariadb-client-core liburing2 libaio1t64 libncurses6 libtinfo6` into /home/z/mariadb-debs, then `dpkg -x` server+client cores into /home/z/mariadb/root/ and the 4 lib debs into /home/z/mariadb/libs/ → /home/z/mariadb/root/usr/sbin/mariadbd, root/usr/bin/{mariadb,mariadb-install-db,…}, libs/usr/lib/x86_64-linux-gnu/{liburing.so.2,libaio.so.1t64,libncurses.so.6,libtinfo.so.6}. Note: Debian ships share files under root/usr/share/mariadb (NOT share/mysql) — mariadbd resolves them fine from --basedir alone, so the watchdog's flag set needs no changes.
   205→- Fresh datadir init (old one unrecoverable): `mariadb-install-db --no-defaults --basedir=/home/z/mariadb/root/usr --datadir=/home/z/mariadb/data --auth-root-authentication-method=normal --skip-test-db` → exit 0. Foreground smoke test with the watchdog's exact flags → "ready for connections" 11.8.6 on 127.0.0.1:3306 + socket /home/z/mariadb/mysqld.sock.
   206→- Started mariadbd, created `CREATE DATABASE IF NOT EXISTS utech; CREATE USER IF NOT EXISTS 'utech'@'%' IDENTIFIED BY 'utech123'; GRANT ALL ON utech.* TO 'utech'@'%'; FLUSH PRIVILEGES;` via root socket auth.
   207→- Exported DATABASE_URL=mysql://utech:utech123@127.0.0.1:3306/utech (sandbox file: override gotcha) → `bunx prisma migrate deploy` (all 29 migrations incl. number_sequence applied) → `node prisma/seed.js` exit 0, idempotent (admin password only set on create; "Done. Login with admin@utech.local / Admin@123").
   208→- API verify: POST /api/auth/login → 200 {token, user SUPERADMIN} through BOTH 127.0.0.1:4000 and 127.0.0.1:3000 (proxy); GET /api/auth/me Bearer → 200 with permissions[]; GET /api/parties → 200 seeded rows (P-0010 Vertex Projects…); GET /health → 200.
   209→- Watchdog handover: `pkill -x mariadbd` + waited 25s → watchdog tick respawnd mariadbd at 15:45:50 as pid 1938 with PPID 1100 (next-server child → survives bash sessions); "ready for connections" in erp-services.log; no spawn errors, no spawn storms; re-verified login 200 after handover.
   210→- Browser verify (agent-browser 0.35.0 headless Chromium): open http://localhost:3000/ → ERP login page (title "UTech Automation ERP", /login after auth-guard redirect); filled admin@utech.local / Admin@123 → redirected to / Dashboard rendering REAL data: Sales(30d) ₹65,254.00 / 4 invoices, Active Jobcards 0, Pending Jobwork 0, Overdue Invoices 0, Total Parties 10, Total Items 10, Sales Performance bar chart (Apr–Sep), Inventory Distribution donut, Production Efficiency empty-state text — no crash boundary; `agent-browser errors` empty. Screenshot: qa-shots/5a-dashboard.png. Also loaded /invoices → all 5 seeded invoices (INV-1001…INV-1005 with PARTIALLY_PAID/DRAFT/ISSUED states).
   211→
   212→Stage Summary:
   213→- Root cause: /home/z/mariadb (MariaDB 11.8.6 user-space install + datadir) had been erased from disk → watchdog spawn ENOENT → 3306 down → Prisma "Can't reach database server" → auth.controller login → 500 {"error":"InternalError"}. Proxy/backend/Next.js were never at fault.
   214→- Fix: rebuilt user-space MariaDB 11.8.6-0+deb13u1 from Debian trixie debs at the exact watchdog paths (root/usr for server+client, libs/usr/lib/x86_64-linux-gnu for liburing2/libaio1t64/libncurses6/libtinfo6), fresh mariadb-install-db init at /home/z/mariadb/data, recreated DB utech + user utech/utech123, prisma migrate deploy (29 migrations) + idempotent seed. Zero changes to Next.js files, backend code, or the watchdog; watchdog then adopted mariadbd as a next-server child (pid 1938, PPID 1100).
   215→- Verified: login 200 via :3000 and :4000; /api/auth/me + /api/parties 200 with data; browser UI login → dashboard with real KPIs/charts; ports 3306/4000/3000 all listening; debs kept at /home/z/mariadb-debs for future re-extraction.
   216→- Caveats: (1) DB content is the fresh seed baseline — QA-era artifacts from previous sessions (party P-0011, ₹500 payment on INV-1005) are gone because the datadir was wiped before this session; fresh seed is reconciled per Task 7 (line-consistent totals, INV-1005 ₹500 CASH payment row). (2) If the platform wipes /home/z/mariadb again, the same ENOENT loop returns — but binaries+datadir now persist on disk, so watchdog auto-revival covers normal sandbox restarts. (3) Watchdog portUp probes still cause harmless MariaDB "Aborted connection" warnings.
   217→---
   218→Task ID: 6-a
   219→Agent: ux-foundation-agent
   220→Task: Shared UX primitives + layout fixes
   221→Work Log:
   222→- Style scan: tailwind.config.js (brand=violet primary, danger=red destructive, slate muted; GOTCHA emerald aliased to violet, cyan to orange), src/styles.css (.btn-*/.input/.label/.card/.badge component classes, global webkit scrollbar, fadeIn/slideUp/scaleIn animations), react-hot-toast, PageHeader/DataTable/Badge/Pagination convention in src/components/ui. Inventoried every form surface via rg over pages+components (see docs/FORMS_UX_PLAN.md §3 for the full table: 56 files, counts of select/input/textarea each).
   223→- Wrote /home/z/my-project/utech-repo/docs/FORMS_UX_PLAN.md — the contract for waves B/C/D: (a) full inventory table with one-line state notes, (b) design-system spec (token mapping + every primitive API + usage patterns), (c) wave assignments (B=sales/masters: quotations, invoices, sales returns, back orders, parties, items; C=ops/purchase/admin: GRN, PO, purchase returns, quality, dispatch, jobcards+components, jobwork, vendor work orders+modals, expenses, users, roles, departments(+subcategories), machines, processes, production+shifts, BOMs, customer material, assignments+AssignModal, projects; D=dashboard/shell/read-only: Dashboard, OperatorDashboard, Login, NotFound, DataTable/Pagination/PageHeader/Badge, Reports, 3 analytics, stock, AppLayout, NotificationBell, ChangePasswordModal), (d) rules for the waves (no new palette, SearchableSelect for entity selects, ConfirmDialog for deletes, EmptyState for empties, actionsBar for form footers).
   224→- Built primitives (plain JSX, zero new deps, app palette only): src/lib/formStyles.js (single `styles` export: input/inputError/textarea/label/hint/errorText/selectBtn/primaryBtn/secondaryBtn/ghostBtn/dangerBtn/formGrid/formGrid3/formGrid4/actionsBar/sectionCard), src/lib/validation.js (required/email/phone10+normalizePhone/gstin/pincode/positiveNumber/gstRate/hsn/passwordStrong + validators map + validateAll(values,schema)→{errors,ok}; validators return error-string|null, format validators allow empty), src/lib/useClickOutside.js (ref+onAway+{active,escape,events}; outside mousedown/touchstart + Escape).
   225→- Built UI components in src/components/ui/: FormField.jsx (label+red required asterisk+hint+error w/ AlertCircle, deterministic ARIA ids `${id}-hint`/`${id}-error`), FormSection.jsx (card-flat section, icon in soft brand circle, title/description/actions slot, p-4 sm:p-5), SearchableSelect.jsx (button-triggered filterable listbox: case-insensitive substring filter over label+subtitle, ArrowUp/Down/Home/End/Enter/Escape/Tab, ARIA listbox/option/aria-expanded/aria-activedescendant, outside-click+scroll-close via capture listener that IGNORES the listbox's own scrolling, Escape in capture phase so it closes only the popover and not an enclosing Modal, clear × when allowClear+value, check icon + active highlight + scrollIntoView, max-h-60 list, loading row, matches-count when >50, trigger shows selected LABEL never raw id, loose String() value comparison, empty error string → '' on clear), Modal.jsx (portal to body, ref-counted body scroll lock for stacked modals, focus trap w/ Tab cycling + initial focus + focus restore, Escape close unless disableEscape, overlay click unless closeOnOverlay, sizes md/lg/xl/full, animate-fade-in overlay + animate-scale-in panel, role=dialog aria-modal aria-labelledby, X button, z-[70]), ConfirmDialog.jsx (on Modal: destructive variant w/ red AlertTriangle + dangerBtn, loading spinner + disabled buttons, Enter=confirm capture listener that skips button/textarea targets, Escape=cancel via Modal), EmptyState.jsx (muted icon in soft circle, title/description, primary sm CTA).
   226→- Layout fixes: AppLayout.jsx user dropdown now closes on outside click AND Escape via new shared useClickOutside hook (was toggle-only). NotificationBell.jsx rebuilt: items are now clickable → routeForNotification(n) maps refType→detail route (VENDOR_WORK_ORDER/:refId, JOBCARD/:refId) then type-prefix fallback (ASSIGNMENT→/assignments, INVOICE→/invoices/:refId, LOW_STOCK→/items, GRN/DISPATCH/returns/PO→section pages), closes popover before navigating, mark-one-read + mark-all-read behavior preserved; popover also closes on Escape now (shared hook). Login/NotFound untouched (wave D).
   227→- Verification: bunx eslint on src/lib + src/components/ui + src/components/layout → 0 problems in new files (only pre-existing Badge.jsx STATUS_ICONS warning); full `bun run lint` → 0 errors, 71 warnings, all pre-existing (matches Task 7's count), none introduced. Sanity compile: per-file esbuild --bundle over all 11 new/changed files → all resolve+transform clean. bunx vite build --outDir /tmp/ux-foundation-build --emptyOutDir → green (2426 modules; only the pre-existing >500kB chunk warning), built CSS contains the new utility classes, temp dir deleted. No form pages modified, nothing deployed, no git commits.
   228→Stage Summary:
   229→- New files: frontend/src/components/ui/{FormField,FormSection,SearchableSelect,Modal,ConfirmDialog,EmptyState}.jsx, frontend/src/lib/{formStyles,validation,useClickOutside}.js, docs/FORMS_UX_PLAN.md. Modified: components/layout/AppLayout.jsx (user menu outside-click/Escape), components/layout/NotificationBell.jsx (clickable notifications + Escape close).
   230→- formStyles contract: `import { styles } from '<rel>/lib/formStyles'` — styles.input (+ styles.inputError concat on error), styles.textarea, styles.label, styles.hint, styles.errorText, styles.selectBtn, styles.primaryBtn/secondaryBtn/ghostBtn/dangerBtn, styles.formGrid/formGrid3/formGrid4 (put FormFields as direct children; full-width field = className "sm:col-span-2"), styles.actionsBar (sticky footer bar), styles.sectionCard.
   231→- Component APIs (all default exports): <FormField id label required hint error className> wraps any control; <FormSection icon title description actions>; <SearchableSelect value onChange(raw value|'') options=[{value,label,subtitle}] placeholder disabled error loading allowClear emptyText className id>; <Modal open onClose title description size='md'|'lg'|'xl'|'full' footer closeOnOverlay disableEscape>; <ConfirmDialog open onClose onConfirm title message confirmLabel cancelLabel variant='default'|'destructive' loading>; <EmptyState icon title description action={{label,onClick,icon}}>; useClickOutside(ref, onAway, {active, escape, events}).
   232→- validation contract: each fn → error-string|null; empty valid for email/phone10/gstin/pincode/gstRate/hsn/passwordStrong; required(v,label) + positiveNumber(v,label,{min,max}) fail empty; validateAll(values,{field:(v,all)=>msg|null}) → {errors,ok}; normalizePhone(v) for payload cleanup.
   233→- Waves B/C/D: read docs/FORMS_UX_PLAN.md BEFORE touching any form — §2 is the API contract, §4 the file assignments, §5 the rules (no new palette; SearchableSelect for entity selects; native <select> only for ≤8 static enums; ConfirmDialog replaces window.confirm; EmptyState for page-level empties). SearchableSelect must keep working inside Modal (Escape layering handled) and inside overflow containers (popover is absolute, not portaled — fine for forms, avoid using it in sticky/transform contexts without testing).
   234→- Known pre-existing lint warnings: 71 (react-hooks/exhaustive-deps on page-level load effects + Badge STATUS_ICONS) — leave alone.
   235→---
   236→Task ID: 6-c2
   237→Agent: wave-c2-production-forms-agent
   238→Task: Professional forms — production/jobcards/VWO (Wave C2)
   239→Work Log:
   240→- Setup reads done in order: worklog.md from line 197 (Task 6-a contract), docs/FORMS_UX_PLAN.md fully, then each primitive (FormField/FormSection/SearchableSelect/Modal/ConfirmDialog/EmptyState, lib/formStyles, lib/validation, lib/useClickOutside) plus DataTable/PageHeader/Pagination, tailwind.config.js (emerald→violet alias gotcha confirmed), lib/format.js and reference upgraded form pages/parties/PartyForm.jsx for pattern alignment.
   241→- Scope audit on arrival: the VWO cluster (VendorWorkOrderForm/VendorWorkOrdersPage/VendorWorkOrderView, SendToVendorModal, RecordReturnModal, VendorWorkOrderTrail) was ALREADY on the new primitives (FormSection/FormField/SearchableSelect/styles, validate-style inline errors, EmptyState) — so for those the pass was verification + one fix: VendorWorkOrderForm footer Cancel switched ghostBtn→secondaryBtn per the actionsBar contract. All other files were raw and got the full pass.
   242→- JobcardForm.jsx (full rebuild, visual/UX only): FormSection cards "Basic Details" (formGrid3: date, party, qty ordered, engineer, project/assembly/drawing numbers, start/end, remarks textarea), "Materials (BOM)" (line table, Add material action) and "Drawing Documents" (3 category upload boxes). Party/engineer/line-item lookups → SearchableSelect (party "code — name", engineers filtered to role 'Project Engineer' exactly as before); the item select's `__new__` option was replaced by the (already existing) inline "+ new item" editor (name input + UoM SearchableSelect + confirm/cancel) — add-new flow preserved via the Plus button. Submit validation: date required, qtyOrdered positiveNumber, per-line qty positiveNumber for lines-in-play with inline errorText + focus-first-error; errors clear as fields change. window.confirm file delete → destructive ConfirmDialog (loading state, same endpoint). actionsBar footer with secondaryBtn cancel + primaryBtn save (Loader2 spinner, disabled while saving, existing `saving` flag kept). Payload/endpoints byte-identical (partyId/itemId/operator/engineer Number() coercion, staged-file upload sequencing, pendingDocs flush, operations:[]); qtyOrdered default stays 0 so untouched submits still send 0 — validation now forces the user to set a positive qty before saving.
   243→- JobcardsPage.jsx: window.confirm delete → destructive ConfirmDialog that keeps the two-step semantics (cancelled rows = permanent delete, else mark-CANCELLED) with matching confirm labels and identical toasts; DataTable gets filtered={search||status} plus custom empty title/description/CTA (New Jobcard) for the zero-row unfiltered case; toolbar unchanged (search + 6-option status native select — within the ≤8 static enum rule).
   244→- JobcardView.jsx: window.confirm × 2 → ConfirmDialog (delete attachment destructive w/ loading; mark-completed default variant, confirmLabel "Mark Completed", completing flag reused) — same endpoints/toasts. Palette gotchas fixed: stat cards text-blue-600→text-brand-600, text-emerald-600→text-success-600, text-red-600→text-danger-600; page-coverage bars bg-emerald-500→bg-success-500, bg-blue-500→bg-brand-500 (+ legend spans). Loading spinner → Loader2. All data logic (page scoping, pageCoverage buckets, takenPages map, assignment summaries) untouched.
   245→- Jobcard widgets (light restyle only, data logic untouched): ChecklistWidget — added "N of M done" counter, tidier hover rows, EmptyState (no CTA) when checklist empty; WorkUpdatesPanel — styles.input/primaryBtn with busy spinner, border card rows, EmptyState when no notes, Loader2 loading row; ActivityTimeline — Loader2 loading row, EmptyState when no events, timeline rail/dots unchanged.
   246→- JobworkForm.jsx (full rebuild): FormSection "Challan Details" (owner-type pills kept, date, vendor SearchableSelect span-2, customer-material-lot SearchableSelect — label "inwardNumber — materialDescription", subtitle "Available: N" — shown only for CUSTOMER, expected return, notes textarea) + "Material Sent / Items" line table with SearchableSelect item picker (company) or description input (customer), qty/rate/notes inline inputs with per-line inline errors. validateAll schema (date, vendor, customer-lot required only when CUSTOMER) + per-line rules: item required for company lines-in-play / description required for customer lines, qty positiveNumber, rate positiveNumber when filled; errors clear on change, focus-first-error. actionsBar + saving spinner. Payload identical (incl. rate 0→null falsy behavior and unfiltered lines).
   247→- JobworkPage.jsx: hand-rolled receive overlay → Modal (size lg) with footer Cancel/Receive (secondary/primary, Loader2, saving flag); the submit-time outstanding-overrun guard kept byte-identical plus live inline row error ("Max N outstanding" + inputError) computed from the same outstanding formula; DataTable empty → custom EmptyState CTA (New jobwork); Customer/Company badge purple-* → brand-* per palette rule.
   248→- AssignModal.jsx: rebuilt on Modal (md, footer Cancel + "Assign Work" primary w/ spinner) keeping the department multi-select checkbox UI, per-dept eligible-user fetch and the exact Promise.all payload; per-dept user picker native select → SearchableSelect (label name, subtitle role), priority kept as native select (4 static enums) with styles.input, due date/instructions wrapped in FormField + formGrid; same validation toasts.
   249→- Verification: bunx eslint over all 15 scoped files → 0 errors, 6 warnings (all pre-existing react-hooks/exhaustive-deps page-load-effect warnings of the same class as the 71 baseline); esbuild per-file bundle sanity → all resolve (only the pre-existing pdfjs `?url` quirk surfaces, unrelated to these changes); bunx vite build → green (only the pre-existing >500kB chunk warning); temp build dir deleted. No files outside the 15 touched, no new deps, no git commands.
   250→Stage Summary:
   251→- Changed (10): pages/jobcards/JobcardForm.jsx, JobcardsPage.jsx, JobcardView.jsx; components/jobcards/{ChecklistWidget,WorkUpdatesPanel,ActivityTimeline}.jsx; pages/jobwork/JobworkForm.jsx, JobworkPage.jsx; components/assignments/AssignModal.jsx; pages/vendorWorkOrders/VendorWorkOrderForm.jsx (one-line ghostBtn→secondaryBtn).
   252→- Verified already-conformant, untouched (5): pages/vendorWorkOrders/VendorWorkOrdersPage.jsx, VendorWorkOrderView.jsx; components/vendorWorkOrders/{SendToVendorModal,RecordReturnModal,VendorWorkOrderTrail}.jsx.
   253→- Patterns applied: FormSection cards + formGrid(3) + FormField with hint/error/ARIA, SearchableSelect for all entity pickers (parties, users, items, uoms, customer lots, assignment users), native select only for ≤8 static enums (status filter, priority) with styles.input, validateAll/positiveNumber inline validation w/ clear-on-fix + focus-first-error, actionsBar footers with Loader2 saving state, ConfirmDialog for every destructive confirm (jobcard delete, attachment delete ×2, mark-completed), EmptyState for zero-row lists and widget empties, violet/slate/success/danger palette only (blue/emerald/purple strays purged in scope).
   254→- Behavior notes for the record (not fixed, by design): (1) JobcardForm now blocks submit until Qty Ordered > 0 — previously qty 0 was silently sent; (2) jobwork company lines-in-play now require an item and rates must be positive — previously null/negative could be sent; (3) JobworkPage pre-existing gap kept: /parties?type=VENDOR misses BOTH-type vendors (comment in code); (4) JobcardView "revert" keeps its inline reason form without a confirm dialog (it had none before; the reason field is the guard).
   255→- Lint: 0 new errors; 6 warnings, all pre-existing exhaustive-deps class.
   256→---
   257→Task ID: 6-c1
   258→Agent: wave-c1-purchase-forms-agent
   259→Task: Professional forms — purchase/ops (Wave C1)
   260→Work Log:
   261→- Read worklog 6-a entry + docs/FORMS_UX_PLAN.md (§2 API contract, §4 assignments, §5 rules), then all 9 primitives once (formStyles, validation, useClickOutside, FormField, FormSection, SearchableSelect, Modal, ConfirmDialog, EmptyState) to use the APIs exactly.
   262→- State on arrival: GRNForm, PurchaseOrderForm, PurchaseOrdersPage, GRNView, PurchaseOrderView had ALREADY been migrated to the primitives by an earlier (unlogged) run — audited each against the contract instead of redoing them: removed the dead `blur`/`touched` leftovers they contained (show() now keyed on `submitted` only). Rewrote the 3 untouched forms and tidied the 3 list pages (11 files total, nothing else touched).
   263→- PurchaseReturnForm.jsx (was fully raw): GRN/PO/Vendor selects → SearchableSelect (GRN number + party subtitle over ACCEPTED GRNs, PO number over APPROVED/RECEIVED, vendor code — name + phone; mutually exclusive GRN/PO kept, all three disabled in edit mode); FormSection ×2 (Undo2 "Return Details" / ListPlus "Return Items") with FormField wrappers + hint/error; validateAll schema on submit (partyId+date required, per-line positiveNumber on return qty) with inline errors + focusFirstError + toast, errors clear as user fixes; items table restyled (styles.input, right-aligned tabular amount, responsive 12-col grid, EmptyState before a GRN/PO is picked); window.confirm approve/process → 2× ConfirmDialog with acting spinner (endpoints unchanged); alert('Error saving…') → toast.error with backend message; actionsBar footer (Approve/Process left via sm:mr-auto, Cancel/Save right, save disabled+Saving… while loading); removed dead allItems state + unused loadAllItems fetch; payload built exactly as before ({...formData, parseInt ids, lines spread with parseInt/parseFloat}).
   264→- QualityPage.jsx (was hand-rolled fixed overlay): modal → <Modal size="xl"> with footer Cancel/Save (Loader2 spinner); FormSection "Inspection Details" (formGrid3: date, refType enum select, ref SearchableSelect, result, inspector, cert no, remarks) + FormSection "Inspection Parameters" (styles.input line table, Add via section actions + EmptyState when zero rows); ref picker loads options from /jobcards | /grns | /dispatch (pageSize 100, refetched on open + refType change, abort-guarded, subtitle = party • status) replacing the raw Ref ID number input — refType switch now clears refId (hint documents it); validation via validateAll (Reference required) replacing the ad-hoc refId>0 check; delete window.confirm → destructive ConfirmDialog with deleting spinner; POST /quality payload unchanged ({...editing, refId Number, parameter-filtered lines}); wrapped Modal render in {editing && …} so modal children never evaluate with editing=null; DataTable empty CTA added.
   265→- DispatchForm.jsx (was raw): FormSection ×2 (Truck details / Package items) on styles.formGrid4; customer + per-line item → SearchableSelect (item label code — name, subtitle "N uom in stock"; picking an item still auto-fills UoM); FormField wrappers everywhere; validateAll (customer+date required, per-line item required + qty positiveNumber) with inline line errors + focusFirstError; silent `catch {}` on save now logs + toast.error('Failed to save dispatch') (UX-only error feedback, endpoint/payload unchanged); items table restyled with per-row delete kept and dashed Add-line footer; actionsBar with Cancel/Save(spin).
   266→- Pages: GRNsPage (status column now Badge; DataTable emptyTitle/emptyDescription/emptyAction → "New GRN" CTA), PurchaseOrdersPage (empty CTA "New PO"), PurchaseReturnsPage (toolbar search/status → styles.input, total chip kept, empty CTA "New Return", total rendered font-mono tabular), DispatchPage (empty CTA "New dispatch" via useNavigate; status-change silent catch now toasts error; success toast copy friendlier). Views: GRNView (status now Badge, meta grid gap-y tidy, notes block separated by hairline), PurchaseOrderView (Create GRN action got a Plus icon).
   267→- Verify: bunx eslint over the 11 files → 0 errors, 4 warnings (all pre-existing react-hooks/exhaustive-deps load/loadReturn patterns, present in the baseline; baseline had 10 warnings — net −6, none added). Per-file esbuild --bundle sanity over all 11 → clean. Confirmed no window.confirm/alert and no raw `.input`/`.label` classes remain in scope. No vite build, no git commands, no edits outside the 11 files.
   268→Stage Summary:
   269→- Files changed (frontend/src/pages/**): purchase/{GRNForm,GRNView,GRNsPage,PurchaseOrderForm,PurchaseOrderView,PurchaseOrdersPage}.jsx, purchaseReturns/{PurchaseReturnForm,PurchaseReturnsPage}.jsx, quality/QualityPage.jsx, dispatch/{DispatchForm,DispatchPage}.jsx — 11/11 in scope.
   270→- Patterns applied: FormSection+FormField+formGrid* layout, styles.* input/textarea/buttons/actionsBar everywhere, SearchableSelect for every entity lookup (vendor/customer/PO/GRN/item/jobcard-ref), validateAll schemas with inline clear-on-fix errors, ConfirmDialog (destructive for QC delete, default for return approve/process), EmptyState in form sections and DataTable zero-rows with CTAs, saving/acting flags with spinner + disabled buttons. Palette untouched (brand/slate/danger only via primitives).
   271→- Preserved: all endpoints, payloads, status logic, GRN/PO mutual exclusion, dispatch stock-out semantics, permission-gated approve/reject on PurchaseOrdersPage, double-submit guards, navigation. GRNForm/POForm pre-migrated logic untouched beyond dead-code removal.
   272→- Skipped/notes: (1) no sales-order/jobcard picker exists in DispatchForm (plan §3 mentioned "invoice select") — only customer+item pickers exist, so nothing to upgrade there; (2) PurchaseReturnForm rate stays optional exactly as before (empty rate → null in JSON, pre-existing backend concern, noted not "fixed"); (3) QualityPage DataTable result badges keep the historic PASS→PAID / FAIL→OVERDUE remaps to avoid visual logic changes (PENDING now has its own STATUS_COLORS key but I left the remap as-is); (4) runtime browser smoke test not run (servers/ports shared with other agents) — ESLint + esbuild-compile verification only; (5) 4 exhaustive-deps warnings intentionally left (pre-existing pattern across the repo, per plan).
   273→---
   274→Task ID: 6-c3
   275→Agent: wave-c3-admin-forms-agent
   276→Task: Professional forms — admin/masters (Wave C3)
   277→Work Log:
   278→- Read worklog 6-a section + docs/FORMS_UX_PLAN.md (§2 API contract, §4 assignments, §5 rules), then all six ui primitives + lib/formStyles.js + lib/validation.js + lib/useClickOutside.js to use the APIs exactly. Read all 18 in-scope files first; captured eslint baseline for the 18 files (0 errors, 21 pre-existing warnings).
   279→- Found src/pages/expense/ExpensesPage.jsx ALREADY fully migrated on arrival (Modal + ConfirmDialog + EmptyState + FormField + SearchableSelect + styles + validateAll, inline blur validation, focusFirstError) — verified against the checklist, left untouched (zero-churn).
   280→- UsersPage: rebuilt all 4 hand-rolled fixed overlays (edit user + role/dept/department-role quick-adds) on <Modal> (lg for user form with FormSection "Basic Details" + "Role & Reporting", md for quick-adds); role/department/subcategory/reporting-to/head/parent-role selects → SearchableSelect (filter bar included); window.confirm delete → destructive ConfirmDialog preserving the deactivate-then-delete two-step wording; validation: name/email/phone10/password (required+passwordStrong on create, optional on edit) with inline FormField errors cleared per keystroke; quick-adds validate name/code/hierarchyLevel (0 allowed = highest rank); added saving flags + spinner to all 4 saves; kept payloads, role→department cascade resets, reporting-candidates/subcategory reload effects, canDelete gating exactly as before.
   281→- RolesPage: rebuilt hand-rolled overlay on <Modal size="xl">; FormSection "Role Details" (name/code/description/hierarchyLevel/parent-role SearchableSelect excluding self + Active/Requires-department/Scope checkboxes) + FormSection "Permissions" keeping the chip-matrix logic (togglePerm untouched) with per-module granted-count headers; window.confirm delete → ConfirmDialog; validateAll on submit (name + hierarchyLevel≥0, replaced old toast-only check); saving flag added; EmptyState for zero roles; payload construction unchanged.
   282→- DepartmentsPage + DepartmentSubCategoryPage: modals rebuilt on <Modal>, deletes → destructive ConfirmDialogs, department-head and department selects → SearchableSelect (also the subcategory filter bar), validateAll (dept name; subcat department+name), saving flags, isActive Badge remap (PAID/DRAFT) replaced with direct ACTIVE/INACTIVE statuses; operator/user counts right-aligned.
   283→- MachinesPage/BomPage/ProductionPage/CustomerMaterialPage: DataTable empty states upgraded with emptyTitle/emptyDescription/emptyAction CTAs ("New Machine"/"New BOM"/"New Batch"/"New inward"); status Badge remaps simplified to direct status keys (task 6-d added the missing STATUS_COLORS entries); qty/amount columns right-aligned + tabular-nums.
   284→- MachineForm/BomForm/CustomerMaterialForm: full-page forms rebuilt on FormSection cards + formGrid + FormField + styles.input/textarea + actionsBar footer with spinner; validateAll on submit (code/name; BOM: per-row component item + qty>0 errors keyed item_N/qty_N; customer-material: customer/date/description/qty>0 + optional weight>0); native selects for entities → SearchableSelect (finished item, component items, customer, jobcard, catalog item); alert() swallowed errors → toast.error(err.response?.data?.message || …); component/material row grids restyled with labelled headers, danger-600 remove buttons, aria-labels.
   285→- ProcessPage: modal rebuilt on <Modal> with FormField grid (code locked on edit with hint, stdTime/ratePerHour optional-positive validation preserving null-on-empty payload), ConfirmDialog for deactivate, saving flag; NOTE: save() previously swallowed errors with bare catch{} and remove() had no catch — added standard error toasts to match every other page (behavior improvement, flagged in Stage Summary).
   286→- ProductionForm: rebuilt on FormSections "Batch Details"/"Material Consumption"; shift/jobcard/item/bom/machine selects → SearchableSelect (edit mode renders read-only disabled inputs showing the selected label, preserving the isEdit lock); per-row material validation (item required, planned qty ≥ 0 non-empty, consumed ≥ 0); Start flow: window.confirm → ConfirmDialog; Complete flow: two prompt() calls → proper <Modal> with qtyProduced (positiveNumber) + qtyRejected (≥0) and server-message error toasts; payload construction (parseInt/parseFloat map, materialConsumptions) and the post-creation per-row consumption save endpoint untouched.
   287→- ShiftsPage: modal rebuilt on <Modal> with FormField grid + validateAll (code/name/start/end required), saving flag, EmptyState with "New Shift" CTA when no shifts; PALETTE GOTCHA FIX: active pill used bg-teal-100/text-teal-700 which tailwind.config aliases to ORANGE — replaced with success-100/success-700.
   288→- CustomerMaterialView: action overlay → <Modal> with FormField qty/notes, validateAll (qty>0, ≤ availableQty), actingBusy double-submit guard + server error toast (runAction previously had bare catch{}); PALETTE GOTCHA FIX: text-emerald-600 renders VIOLET in this config (emerald aliased to brand) — stats/ledger colors moved to success-600/danger-600 + tabular-nums.
   289→- ProjectsPage/ProjectView: project modal rebuilt on <Modal lg> with FormField grid + customer SearchableSelect + native status enum select; validateAll (name, optional budget>0, end≥start); ConfirmDialog replaces window.confirm; saving flags; budget right-aligned; ProjectView add-task overlay → <Modal> with name-required validation, task delete → ConfirmDialog, zero-tasks → EmptyState with "Add task" CTA, row status select kept native (4 static enums) with styles.input; setStatus left without try/catch (pre-existing; noted).
   290→Stage Summary:
   291→- Files changed (17 of 18; ExpensesPage arrived already migrated and was verified untouched): frontend/src/pages/users/UsersPage.jsx, roles/RolesPage.jsx, departments/{DepartmentsPage,DepartmentSubCategoryPage}.jsx, machines/{MachinesPage,MachineForm}.jsx, process/ProcessPage.jsx, production/{ProductionPage,ProductionForm,ShiftsPage}.jsx, bom/{BomPage,BomForm}.jsx, customerMaterial/{CustomerMaterialPage,CustomerMaterialForm,CustomerMaterialView}.jsx, project/{ProjectsPage,ProjectView}.jsx. No files outside the 18 touched; primitives/libs/docs untouched; no new deps.
   292→- Patterns applied: styles.input/textarea/selectBtn + inputError concat, FormField everywhere with ARIA ids, FormSection cards with icons, actionsBar footers (spinner + disabled while saving), SearchableSelect for all entity lookups (native select only for ≤8 static enums: machine status, project status, user status filter, task status), destructive ConfirmDialog for ALL deletes/confirms (window.confirm eliminated in scope; prompt()/alert() eliminated too), EmptyState CTAs (page-level + DataTable emptyTitle/emptyAction), amounts/qty right-aligned tabular-nums, violet/slate palette only (fixed pre-existing teal-*/emerald-* alias misuse in my files).
   293→- Lint: bunx eslint over the 18 files → 0 errors, 5 warnings, all pre-existing react-hooks/exhaustive-deps on untouched list-page load effects (ExpensesPage/MachinesPage/BomPage-area/ProductionPage/CustomerMaterialPage); baseline was 21 warnings — reduced by rewrites, none introduced.
   294→- Behavior notes for reviewers (not silently fixed beyond error-surfacing): (1) ProcessPage save used to swallow errors (bare catch) and remove() had no catch — now toasts like every other page; (2) CustomerMaterialView runAction used to swallow errors — now toasts; (3) ProjectView setStatus still has no try/catch (left as-is); (4) UsersPage mount effects fire unguarded role/dept loads (pre-existing); (5) material "planned qty" now requires a non-empty number ≥ 0 on create (previously NaN could reach the API if left blank) — stricter inline validation only, payload mapping unchanged; (6) role permissions payload still spreads the full editing object on PUT (unchanged).
   295→---
   296→Task ID: 8
   297→Agent: pdf-docs-agent
   298→Task: Simple-language ERP flow guide PDF
   299→
   300→Work Log:
   301→- Read report brief + fonts/cover/palette/overflow typesetting specs from skills/pdf, then mapped real modules from source: frontend/src/App.jsx (34 routes), backend/src/routes/ (34 route files), AppLayout.jsx NAV_SECTIONS (real menu labels: Masters/Sales/Company Inventory/Customer Inventory/Stock Management/Analytics/Admin), prisma schema enums (DocStatus, InvoiceStatus, JobcardStatus, JobworkStatus, DispatchStatus, PurchaseStatus, VendorWorkOrderStatus, ReturnStatus, BackOrderStatus), seed.js roles (SUPERADMIN, MANAGER, OPERATOR + Admin/Plant Head/Project Engineer/Department Head/Supervisor/Team Leader; admin@utech.local), numbering.js PREFIXES (QT/JC/JW/DC/INV/P/IT/CML/VWO, format PREFIX-YY-#####), grn.controller (GRN books accepted qty stock IN, auto-advances VWO via PO link), dispatch/production/salesReturn/purchaseReturn controllers via stockService.moveCompanyStock (dispatch draft reserves stock = stock DOWN, production: finished IN + material consumption OUT, sales return IN, purchase return OUT), report.routes (8 XLSX reports), tally.routes (masters + vouchers export, ERP9/Prime 2.0/2.1, manual sync).
   302→- Palette: palette.cascade pins base hue 200 for all intents (cannot do violet) -> re-derived the same tier structure (XL S<=0.08, L<=0.15, M<=0.30, S<=0.50, XS<=0.75) at violet hue ~257-262 matching the product brand (FORMS_UX_PLAN.md brand-600 #7c3aed) + slate neutrals; documented in script header.
   303→- Cover: Template 01 (HUD left anchor line) HTML at 794x1123, Inter + Liberation Sans fallback, white bg + 2% violet grid + 8px violet anchor line; kicker "Simple Flow Guide - How Everything Works", hero "UTech / Automation ERP" 45pt, summary block, meta "U-Tech Automation Industries / Version 1.0 - September 2026". poster_validate check-html PASS (after removing a nested <span> that tripped text-text overlap), cover_validate.js PASS, rendered via html2poster.js --width 794px.
   304→- Body: ReportLab TocDocTemplate + multiBuild (clickable auto-TOC with bookmark keys), A4, symmetric 62pt margins, FreeSerif family + install_font_fallback(), footer rule + "UTech Automation ERP - Simple Flow Guide" + Page N on every body page. Custom Flowable FlowDiagram draws the 9-stage snake flow as pure vector (ReportLab canvas roundRect + arrowhead polygons, violet #7c3aed boxes on #f5f3fa tint, slate arrows; 5 boxes row 1 L->R, vertical connector, 4 boxes row 2 R->L, x span 68..529pt inside 62/533 margins). 10 numbered chapters per spec; all tables light-bordered (HEADER_FILL #4e4170 violet-slate header + white bold text, TABLE_STRIPE odd rows, 0.5pt BORDER grid, all cells Paragraph, hAlign CENTER, proportional colWidths <= AVAIL_W).
   305→- QA loop: code.sanitize before each run; fixed 3 findings from pdf_qa (page-size mismatch cover 595.9 vs A4 -> normalize tolerance 0.1pt + scale_to; diagram drawn with MARGIN offset overflowing right margin -> flowable-local coords; Helvetica leaking from default bulletFontName -> FreeSerif on bullet/step styles; table em-dash line-start cells -> 'n/a'). Final: meta.brand, pages.clean (0 blank), font.check 0 issues, toc.check + toc_validate PASS, pdf_qa --skip-cover PASS (13 checks).
   306→- Verification: 11 pages total (1 cover + 1 contents + 10 chapters); diagram page confirmed via pymupdf (9 rounded boxes, all 9 stage labels, inside margins); TOC entry page numbers match printed footer numbering (body starts at Page 1 = Contents, cover uncounted).
   307→- Outputs copied byte-identical (150,473 bytes) to download/, public/ (dir created), and utech-repo/backend/public/docs/.
   308→
   309→Stage Summary:
   310→- 11 pages, A4. Sections: Cover; Contents; 1 What is UTech ERP?; 2 The Big Picture (vector 9-stage flow diagram + one sentence per stage); 3 The Sales Journey, Step by Step (8 subsections + summary table); 4 Buying from Vendors, Step by Step (VWO -> PO -> GRN -> quality -> purchase return + GRN callout); 5 Keeping Stock Correct (UP/DOWN table + Stock Overview & Ledger + customer-owned material note); 6 Masters - The Foundation (7-master table); 7 Who Can Do What - Roles & Permissions (role table in plain words); 8 Reports & Tally Export (8 XLSX reports + Tally masters/vouchers paragraph); 9 Glossary (14 terms); 10 Quick Start for a New User (login, change admin password day one, dashboard, first 3 things to try).
   311→- Files: /home/z/my-project/download/UTech_ERP_Flow_Guide.pdf (146.9 KB), /home/z/my-project/public/UTech_ERP_Flow_Guide.pdf (146.9 KB), /home/z/my-project/utech-repo/backend/public/docs/UTech_ERP_Flow_Guide.pdf (146.9 KB); sources in /home/z/my-project/task8-pdf/ (cover.html, cover.pdf, generate_body.py, merge_final.py, body.pdf).
   312→- Mapping honesty notes: (1) No standalone "Sales Order" document exists in the codebase - presented as "the approved quotation is the agreed order" (quotation has CONVERTED status via convertToInvoice). (2) Quality has a backend module + /quality page but NO sidebar menu entry - described as accepted/rejected quantities on GRN + "the Quality page", no invented menu path. (3) diagram stage names kept as requested by spec; stage sentences use real statuses/numbers only.
   313→---
   314→Task ID: 6-b
   315→Agent: wave-b-sales-forms-agent
   316→Task: Professional forms — sales/masters (Wave B)
   317→Work Log:
   318→- Setup reads in order: worklog.md from line 197 (Task 6-a contract), docs/FORMS_UX_PLAN.md fully (§2 API contract, §3 inventory, §4 assignments, §5 rules), then every shared primitive once (formStyles, validation, useClickOutside, FormField, FormSection, SearchableSelect, Modal, ConfirmDialog, EmptyState) plus PageHeader/DataTable/Badge/tailwind.config.js (emerald→violet + teal→orange alias gotchas confirmed) and styles.css (.btn-*/.card-flat/.pd-* print classes).
   319→- Scope audit on arrival: PartyForm, PartiesPage, ItemForm, ItemsPage were ALREADY on the new primitives (earlier unlogged run, same as C1 found) — audited all four against the contract instead of redoing them; one gap fixed: PartyForm had no PAN validation. QuotationForm/QuotationsPage/QuotationView/InvoiceForm/InvoiceView/InvoicesPage/SalesReturnForm/SalesReturnsPage/BackOrderForm/BackOrdersPage were raw and got the full pass. Cleaned 3 now-unused eslint-disable directives in PartyForm/ItemForm load effects.
   320→- QuotationForm.jsx (full rebuild, visual/UX only): FormSection ×3 — "Quotation Details" (party → SearchableSelect over CUSTOMER parties, label name + code·phone subtitle, disabled in edit; date/valid-until date inputs; notes textarea), "Header & Footer" (template picker → SearchableSelect in the section actions slot + delete icon, header/footer textareas with the existing upload/preview image UI byte-kept, save-as-template row), "Line Items" (12-col row grid kept, part-name/qty/rate wrapped in FormField with per-line inline errors, amount readOnly). validateAll schema (party+date required) + per-line required part name / positiveNumber qty+rate; errors clear on fix; window.confirm template delete → destructive ConfirmDialog; alert('Error saving quotation') → toast.error with backend message; added catch+toast to image upload / save-as-template / delete-template (previously silent unhandled rejections); actionsBar footer with Loader2 spinner on Save. Payload/endpoints byte-identical.
   321→- QuotationsPage.jsx: confirm() delete/cancel → ConfirmDialog (CANCELLED rows = destructive "Delete permanently", live rows = default "Cancel quotation", matching the API's soft-cancel semantics, deleting spinner, original toasts kept); page-level EmptyState with "New Quotation" CTA when zero rows and no filters/search (error state still routes to DataTable's retry banner); toolbar selects → styles.input + aria-labels; Total column right-aligned.
   322→- QuotationView.jsx: convert-to-invoice confirm() → ConfirmDialog (default variant, converting spinner + "Creating invoice…" no-print pill); print-doc pd-* markup verified byte-identical to HEAD via git diff (print layout logic untouched); everything else unchanged.
   323→- InvoiceForm.jsx (most complex; calculation + payload untouched): FormSection ×4 — "Invoice Details" (date/dueDate, party → SearchableSelect label "code — name" + GSTIN/phone subtitle, GST-type radios kept native with accent-brand-600, discount), "Line Items" (table kept; per-row item → SearchableSelect "code — name" + HSN subtitle with "— manual —" placeholder semantics preserved via clear; description/qty/rate/gstRate inputs with per-line inline errors; amount right-aligned; btn-icon remove), "Notes & Terms" (two textareas side by side), "Summary" (totals panel + actionsBar). validateAll (date/party required, discount ≥ 0) + per-line description required / positiveNumber qty+rate / gstRate 0–28; totals useMemo, itemId prefill (description/hsn/rate/gstRate), "at least one line item" guard, payload shape (Number coercions, dueDate||null, itemId?Number:null), endpoints, toasts and saving guard all byte-identical.
   324→- InvoiceView.jsx: body restructured into FormSection cards (Invoice Details with Bill-To + meta dl + status Badge in actions; Line Items; Summary with totals panel + amount-in-words; Payments when present) — data/load/PDF logic untouched; PALETTE GOTCHA FIX: "Paid" row text-emerald-700 renders VIOLET in this config → text-success-700; discount red → text-danger-600; hand-rolled payment overlay rebuilt on <Modal size="md"> with FormFields (date/amount/mode/reference + notes textarea exposed — notes was already in the payload), validateAll (date required, amount positiveNumber), paySaving spinner in footer, balance shown in description; payment POST now toasts errors (was a silent bare catch{}); payload byte-identical.
   325→- InvoicesPage.jsx: page-level EmptyState ("New Invoice" CTA) + styles.input toolbar + aria-label; stale-response reqId guard kept; Paid/Total columns right-aligned.
   326→- SalesReturnForm.jsx: FormSection ×2 — "Return Details" (invoice → SearchableSelect over ISSUED/PAID/PARTIALLY_PAID with date·total subtitle, disabled in edit; date; reason/notes textareas readOnly-gated) and "Return Items" (loaded lines; qty/rate wrapped in FormField with inline errors; inr amount + total right-aligned); validateAll (invoice+date required) + per-line positiveNumber qty/rate; approve/process confirm()s → ConfirmDialog (default variant, acting spinner, original wording); actionsBar with Approve/Process left (sm:mr-auto) and Cancel/Save right. Payload, editable gating (DRAFT only), endpoints, toasts identical. NOTE (not fixed): per-line "reason" inputs are collected in state but the payload never sends them — pre-existing backend contract, left as-is.
   327→- SalesReturnsPage.jsx: page-level EmptyState ("New Return" CTA), styles.input toolbar, Total/Refund right-aligned.
   328→- BackOrderForm.jsx: FormSection ×2 — "Back Order Details" (invoice SearchableSelect optional/allowClear, auto-loads invoice lines + partyId exactly as before; party SearchableSelect "code — name"; date/expected/notes) and "Back Order Items" (create mode: per-line item SearchableSelect + ordered qty/rate with inline errors + remove; edit mode: readOnly description/qty/rate + per-line Fulfill action); PALETTE GOTCHA FIX: fulfill button text-teal-700 renders ORANGE in this config → text-success-700/bg-success-50; prompt()-based fulfill replaced with <Modal> "Fulfill line item" (qty FormField, outstanding hint from the same qtyOrdered−qtyFulfilled formula, same >0 and ≤outstanding guards, same POST /back-orders/:id/fulfill {lineId, qty}, Enter submits, fulfilling spinner); alert('Error saving back order') → toast.error; line validation only on create (line fields readOnly in edit). Payload/endpoint behavior preserved.
   329→- BackOrdersPage.jsx: page-level EmptyState ("New Back Order" CTA), styles.input toolbar; dropped the unused inr import (pre-existing dead import).
   330→- Verification: bunx eslint over all 14 scoped files → 0 errors, 11 warnings (every one pre-existing class/pattern present in the originals: react-hooks/exhaustive-deps on page-load effects, pre-existing unused allItems/removeItem/navigate); full bun run lint → 0 errors project-wide, 35 warnings; per-file esbuild --bundle over 12 changed files → all resolve+transform clean; rg confirms zero window.confirm/alert/prompt and zero emerald/cyan/teal/indigo/blue raw classes in scope; git diff confirms QuotationView print block byte-identical. No files outside the 14 touched, no new deps, no git mutations.
   331→Stage Summary:
   332→- Files changed (12 of 14): frontend/src/pages/quotations/{QuotationForm,QuotationView,QuotationsPage}.jsx; invoices/{InvoiceForm,InvoiceView,InvoicesPage}.jsx; salesReturns/{SalesReturnForm,SalesReturnsPage}.jsx; backOrders/{BackOrderForm,BackOrdersPage}.jsx; parties/PartyForm.jsx (PAN validator + uppercase/hint/blur on PAN); items/ItemForm.jsx (lint-directive cleanup only).
   333→- Verified already-conformant, untouched (2): parties/PartiesPage.jsx, items/ItemsPage.jsx (already on primitives by the earlier unlogged run).
   334→- Patterns applied: FormSection cards (icon/title/description/actions) + formGrid* + FormField (hint/error/ARIA) everywhere; styles.input/textarea/selectBtn/primary/secondary/ghost/danger + actionsBar footers with Loader2 spinners and disabled-while-saving; SearchableSelect for every entity lookup (party/item/invoice/quotation-template) with native select only for ≤8 static enums (status filters, payment mode, GST-type radios); validateAll schemas + per-line inline errors with clear-on-fix; destructive ConfirmDialog for template delete, default for cancel/approve/process/convert; page-level EmptyState CTAs on all 4 list pages; palette strictly brand/slate/success/danger (fixed pre-existing emerald-*/teal-* alias misuse in InvoiceView + BackOrderForm).
   335→- Preserved: all endpoints, payload shapes and coercions, InvoiceForm totals/GST math and item prefill, edit-mode field locks, permission checks (none existed in scope beyond PartiesPage's party.delete), toasts, navigation, double-submit guards, QuotationView print logic (pd-* byte-identical).
   336→- Notes for reviewers (flagged, not "fixed"): (1) SalesReturnForm per-line reason inputs are collected but never sent in the payload (pre-existing); (2) stricter per-line validation means rate 0 / blank lines are now blocked on quotations/invoices/returns/back-orders (previously silently submitted) — per the brief's positiveNumber mandate; (3) SearchableSelect popover inside InvoiceForm's overflow-x-auto line table scrolls within the container rather than overlaying the page (works, minor UX caveat inherent to the non-portaled popover); (4) error surfaces added where saves swallows errors silently (quotation template upload/save/delete, payment POST, back-order save alert→toast) — UX-only, contracts unchanged; (5) runtime browser smoke test not run (shared dev servers) — ESLint + esbuild verification only.
   337→