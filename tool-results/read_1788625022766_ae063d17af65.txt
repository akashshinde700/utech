     1→  17 H9: PRESENT-AND-CORRECT — package.json multer ^2.0.2; node_modules/multer is 2.3.0 (bun install WAS run; backend/bun.lock created); axios ^1.7.9 added for tally sync (1.20.0 installed).
     2→  18 Quickies: ALL PRESENT-AND-CORRECT — app.set('trust proxy',1); pagination NaN-safe (?page=abc&pageSize=xyz → 200); invoice list ?partyId=abc → 200 (Number.isFinite guard); dashboard invoicesLast30Days excludes CANCELLED; item create writes OPENING StockLedger row in tx when openingStock>0; vendorWorkOrder purchase-order route stacks requirePermission('purchase.create') AND requirePermission('vendorWorkOrder.update') (verified middleware semantics: per-call any-of, stacked calls = AND); purchase approve() blocks self-approval (creator via AuditLog first 'create' entry) unless SUPERADMIN.
     3→- Verification pipeline: DATABASE_URL exported; MariaDB confirmed up on 3306; node --check on every changed/new backend .js (git diff --name-only loop + new validators + seed) — all pass; prisma migrate status/deploy → 28 migrations, no pending (NumberSequence already applied); prisma generate OK; node prisma/seed.js exit 0 with admin login still working; npx eslint src → exit 0 (0 errors, 0 warnings); backend restarted twice (pkill + watchdog respawn confirmed via ps) with clean boot logs.
     4→- Smoke tests (through 127.0.0.1:3000): login 200+token; GET /api/items 200; tally masters parties 200 XML; tally vouchers 200 XML; dispatch roundtrip stock 180→175→(CANCELLED)→180 + re-cancel 400; payment amount 0/-5 → 400, overpay → 400, valid payment → 200; GET /api/users without token → 401; /health ok; case-insensitive login 200; tally sync SSRF probe → 400. NumberSequence row, DISPATCH_CANCEL ledger row and AuditLog rows (login/tallyExport/create/cancel) verified in DB.
     5→
     6→Stage Summary:
     7→- All 18 intended fix items verified live or in code: 16 PRESENT-AND-CORRECT, 2 completed by this task (tally syncToTally now surfaces guard HttpErrors as proper 400s; invoice addPayment overpay baseline now floors on denormalized amountPaid so legacy/seed data can't be overpaid). Migration applied, prisma client regenerated, seed re-run without resetting the admin credential, backend lint 0/0, all smoke tests green through the port-3000 proxy, services healthy (backend respawns via watchdog, MariaDB up).
     8→- Remaining risks (pre-existing, out of this phase's scope): /uploads static still unauthenticated; item/party update() still spread raw req.body (not in the C2 controller list); H6 PartyLedger still not written by invoices/payments/expenses; M17 negative-stock clamping; xlsx dep (H9 part 2) not bumped; attachment upload/download and jobwork receive verified by code review only (no E2E multipart/receive smoke); OTP emails still not actually sent (dev-only devCode).
     9→---
    10→Task ID: 5
    11→Agent: release-engineer
    12→Task: Commit fixes, package deliverables, write customer report
    13→
    14→Work Log:
    15→- Pre-commit checks: branch is `master` (not main); 106 changed/untracked paths (98 M + 8 untracked). Safety sweep of the full diff for secrets/credentials — clean (no DATABASE_URL/JWT_SECRET/password literals in changes).
    16→- .gitignore verification: existing root/backend/frontend .gitignore files already covered backend/.env, backend/uploads/, backend/*.log, node_modules/, frontend/dist — verified with `git check-ignore -v` (all matched). backend/.env.example already existed with placeholder values (mysql://root:password@localhost:3306/utech_erp, change-me JWT_SECRET) — no action needed.
    17→- .gitignore ADJUSTMENT (1): added `public/` (with comment) to backend/.gitignore — backend/public/ is a regenerable build artifact (copy of the already-ignored frontend/dist for single-origin serving, 2.8MB of hashed bundles). Kept it out of the commit/patch to avoid 2.8MB of minified churn in customer's repo; regenerate with `cd frontend && npm run build && cp -r dist/* ../backend/public/`. Documented in the report's How-to-run section.
    18→- Staged `git add -A`: 107 files (99 M, 8 A: backend/bun.lock, frontend/bun.lock, prisma/migrations/20260905100000_number_sequence/migration.sql, validators backOrder.schema.js + production.schema.js, docs/BACKEND_AUDIT.md, docs/FRONTEND_AUDIT.md, pages/vendorWorkOrders/VendorWorkOrderView.jsx). Re-checked staged list for env/log/node_modules/public/uploads — none staged.
    19→- Commit d050c61629f6467419f1082967f5bc07a071afde "fix: security, stock integrity, UX hardening — full audit remediation" via heredoc: sections Security (mass-assignment whitelists ×9, privilege-escalation guards, attachments+multer 2.x, Tally SSRF+XML, OTP crypto/throttle/no-prod-log, trust proxy), Stock integrity (atomic decrement TOCTOU fix, DISPATCH_CANCEL reversal, jobwork receive caps, NumberSequence migration, OPENING ledger), Financial (invoice lifecycle/overpay/PAID-lock/isIntraState, returns cumulative caps, GRN PO-linkage), Frontend (ErrorBoundary, crash joins, Vendor Work Orders module, numberToWords, analytics errors, fail-closed permissions, 15 double-submit guards, 21 DataTable pages, IST dates, 401 toast, debounce), Ops (seed idempotent, jobcard.progress/stock.adjust seeded, dashboard KPI), Docs. Footer with verification line as specified.
    20→- Verified: `git log --oneline -3` shows d050c61 on top of a2f2e14; `git status --short` fully clean (no untracked logs — gitignore covers them). No push performed.
    21→- Deliverables in /home/z/my-project/download/: (1) utech-audit-docs/ (cp -r of docs/: BACKEND_AUDIT.md 14.4KB + FRONTEND_AUDIT.md 11.4KB); (2) utech-improvements.patch — `git format-patch -1 HEAD --stdout` (458KB, 7,788 lines, applies cleanly onto a2f2e14); (3) utech-improvements.bundle — `git bundle create … HEAD master` (500KB, `git bundle verify` OK: "records a complete history", refs/heads/master = d050c61).
    22→- Wrote /home/z/my-project/download/UTECH_IMPROVEMENT_REPORT.md (customer-facing): what was audited (a2f2e14, stack breakdown), 18-row top-issues table (severity/area/issue/fix grouped Security→Stock→Money→UX), new capabilities (Vendor Work Orders module, permission-aware UI, loading/error states), verification summary (28 migrations, idempotent seed, eslint 0/0, live smoke: dispatch 180→175→180, payment guards 400, tally 200, SSRF 400), how-to-run (backend .env/migrate/seed/dev :4000; frontend vite build → cp dist → backend/public; single-origin; admin@utech.local/Admin@123 change immediately), 8 deferred items, 6 phase-2 recommendations, patch/bundle application instructions.
    23→
    24→Stage Summary:
    25→- Commit: d050c61 on master (107 files, +4,149/−450) — working tree clean, nothing pushed.
    26→- Deliverables: /home/z/my-project/download/{UTECH_IMPROVEMENT_REPORT.md, utech-improvements.patch (459KB), utech-improvements.bundle (500KB, full history), utech-audit-docs/{BACKEND_AUDIT.md, FRONTEND_AUDIT.md}}.
    27→- .gitignore adjustments: backend/.gitignore += public/ (build artifact). .env/uploads/logs/node_modules already covered pre-existing; backend/.env.example already present and committed-as-tracked since a2f2e14.
    28→---
    29→Task ID: 6
    30→Agent: browser-qa
    31→Task: E2E browser verification of UTech ERP
    32→
    33→Work Log:
    34→- Env: agent-browser 0.35.0 headless Chromium against http://127.0.0.1:3000; /health 200 pre-test; screenshots in /home/z/my-project/qa-shots/ (01-16).
    35→- Login page: SPA renders correctly (title "UTech Automation ERP", UTech/U-Tech Automation Industries branding, Email + Password fields, Sign in). No white screen. / redirects unauthenticated users to /login (guard active from first load). PASS.
    36→- Login (admin@utech.local/Admin@123): redirects to / (Dashboard). KPI cards show REAL numbers: Sales(30d) ₹65,354.00 / 4 invoices, Active Jobcards 0, Pending Jobwork 0, Overdue 0, Parties 10, Items 10; Sales Performance bar chart + Inventory Distribution donut render (recharts applications visible with axis labels Apr-Sep, ₹0-80k); Production Efficiency shows proper empty-state text. No stuck skeletons. Console: only benign recharts width/height warnings during mount. PASS.
    37→- Page sweep (all load, skeletons resolve, no crash):
    38→  - /invoices: 5 seeded invoices in table (number/date/party/total/paid/status), search + status filter + "New invoice". /invoices/5 detail: party name "Vertex Projects", GSTIN, PARTIALLY_PAID badge, line table, Subtotal ₹25,500, CGST/SGST ₹2,295 each, Total ₹30,190, Paid ₹15,500, Balance ₹14,690, payments table. DEFECT-A: "Amount in words" is NOT rendered anywhere on invoice detail (feature only exists on QuotationView.jsx:267; InvoiceView has no amountInWordsINR call — grep-verified). DEFECT-B (seed data, not frontend): denormalized totals inconsistent with child rows on ALL 5 seeded invoices — e.g. INV-1005 subtotal ₹25,500 vs single line ₹15,000; payments table shows ₹500 while Paid shows ₹15,500 (legacy denormalized baseline; consistent with Task 4-a-verify notes). Frontend faithfully renders API values.
    39→  - /jobcards: renders, empty state "No records / Create your first record…", count "0 jobcards". DEFECT-C (UX/labeling): sidebar entry and page heading call it "Projects" (AppLayout.jsx:35 {to:'/jobcards', label:'Projects'}) while count text says "jobcards" and column says "PROJECT NO" — AND Admin section has a real "Projects" (/projects), so two identically-labeled nav items point to different pages; Jobcards is effectively unfindable by name.
    40→  - /vendor-work-orders (new page): loads fully — KPI strip (Total/Draft/At Vendor/Partly Returned/Returned/Overdue/On Time % 100%), search, status + vendor filters (vendors listed), checkbox "Still outstanding only", proper table headers (VWO NO…STATUS) and empty state. PASS.
    41→  - /items: 10 seeded items with code/name/type/HSN/UOM/stock/sale rate/GST% + type filter + "Low stock" toggle. PASS. /parties: 10 seeded parties (code/name/type/GSTIN/phone/city) + row delete button. PASS.
    42→  - /reports: 7 report cards (Invoices, Jobcards, Items, Expenses, Customer Stock, Vendor Stock, Stock Ledger) each with Download .xlsx. PASS. /users: filters (roles/departments/department roles populate from DB) + user table (System Admin / SUPERADMIN / Active). PASS. /stock: KPI strip (10 company, 0 customer, 0 at vendor, 0 low stock, 4 finished, 0 inward / 2 outward today) + Stock Ledger rows (incl. DISPATCH/DISPATCH_CANCEL from earlier backend smoke test) + Manual Adjustment section. PASS.
    43→  - Extra sweep (25+ routes incl. quotations, dispatch, boms, purchase-orders, grns, returns, back-orders, customer-material, jobwork, assignments, machines, processes, departments, roles, expenses, /department-subcategories, /analytics/sales|production|inventory): all render correct headings, zero error boundaries, zero page errors.
    44→- Create-flow (Parties): New Party form → name "Browser Test Party", type CUSTOMER → Save → redirected to /parties, row P-0011 present in table (11 rows). Delete: row-level red button → NATIVE browser confirm dialog "Deactivate Browser Test Party?" → accept → toast "Deactivated successfully", API shows isActive=false. DEFECT-D (UX): it is a soft DEACTIVATE, but the list keeps showing the row with NO visual inactive state/marker and no filter to hide deactivated parties — list looks unchanged after delete.
    45→- Responsive: 375x812 — no page-level horizontal overflow (scrollWidth=375); sidebar becomes off-canvas drawer (transform -288px) with working hamburger (opens at x=0 + scrim, Escape closes); invoice table wrapped in overflow-x-auto container (scrollWidth 671 scrolls inside, page doesn't break). 1366x768 — sidebar pinned (288px), no overflow. PASS.
    46→- Session: user menu → "Sign Out" → redirect /login, localStorage fully cleared. Direct /invoices while logged out → bounced back to /login with sign-in form. Guard works. PASS.
    47→- Error boundaries: NONE triggered on any visited route (checked /something went wrong|error boundary|oops/ across all pages). No React crash screens seen.
    48→
    49→Stage Summary:
    50→- PASS: login page render, authentication, dashboard KPIs/charts with real data, all requested list pages (Invoices+detail, Jobcards, Vendor Work Orders, Items, Parties, Reports, Users, Stock), party create flow, native-confirm deactivate flow, mobile 375px off-canvas sidebar + horizontal table scroll, 1366px layout, logout + auth route guard, zero error-boundary screens, zero console errors (only benign recharts mount warnings).
    51→- Defects found:
    52→  A) /invoices/:id — no "Amount in words" on invoice view (only QuotationView has amountInWordsINR wired). Task expected it on invoice detail. Frontend gap.
    53→  B) /invoices/:id — seeded invoices show subtotal/paid that don't reconcile with displayed lines/payments (seed data defect: line sums=15,000 vs denormalized subtotals; payments rows≠amountPaid). Visible as contradictory numbers on screen; frontend not at fault.
    54→  C) Sidebar: Jobcards page is labeled "Projects" (AppLayout.jsx:35, /jobcards) duplicating the real Projects (/projects) nav label; page heading "Projects" vs count "0 jobcards" vs column "PROJECT NO" — confusing/wrong labeling, Jobcards unfindable by name.
    55→  D) /parties delete = soft deactivate (confirm "Deactivate …?") but list gives no inactive visual state or active/inactive filter — deleted party looks identical to live ones.
    56→  E) Minor: /department-roles, /sales-analytics etc. are not real routes (catch-all silently redirects to /); real routes are /department-subcategories and /analytics/sales|production|inventory — URLs only discoverable via sidebar; bookmarking guessed URLs lands on Dashboard with no 404.
    57→- Overall verdict: SYSTEM IS USABLE END-TO-END; no crashes, no broken pages, guards and responsive behavior correct. Recommended fixes: render amount-in-words on InvoiceView, fix Jobcards nav label, add inactive indicator/filter on Parties list, reconcile seed invoice totals.
    58→---
    59→Task ID: 7
    60→Agent: polish-fixer
    61→Task: QA defect fixes A-E + seed reconciliation + redeploy
    62→
    63→Work Log:
    64→- A) InvoiceView amount in words: added `amountInWordsINR` import (same helper QuotationView uses from src/lib/numberToWords.js) and a muted small "AMOUNT IN WORDS: …" line directly under the invoice totals box (ml-auto max-w-sm, text-xs text-slate-500, italic words) rendering from inv.total. Verified on /invoices/5: "Seventeen Thousand Seven Hundred Rupees Only".
    65→- B) Seed reconciliation (backend/prisma/seed.js): removed all hand-written subtotal/cgst/sgst/igst/total/amountPaid from invoiceData. Each invoice now declares `lines` + `payments`; header money is computed with the SAME helpers the invoice controller uses (requires ../src/utils/gst.js → calcTaxes/calcLineAmount/round2; total = taxes.total - discount). Payment rows are created only when the invoice has none (idempotent), and a reconcile pass recomputes subtotal/cgst/sgst/igst/total from the invoice's ACTUAL lines (legacy DBs keep their lines) and amountPaid/status from its ACTUAL payment rows (paid>0 → PARTIALLY_PAID/PAID; paid=0 keeps DRAFT/ISSUED). Per-invoice lines chosen to reproduce the originally intended subtotals (INV-1001 12×1500=18000, INV-1002 10×760=7600, INV-1003 300×14=4200, INV-1004 1×12400=12400, INV-1005 17×1500=25500 — also fixes the old INV-1005 total typo 30190→30090 on fresh seeds).
    66→  One-off script /home/z/tmp/fix-invoice-totals.js (PrismaClient + calcTaxes, nothing hardcoded) run once against the live DB. Before → after per invoice (all have one legacy line 10×1500@18% = ₹15,000):
    67→    INV-1001 (id 1): subtotal 18000→15000, cgst 1620→1350, sgst 1620→1350, total 21240→17700, amountPaid 5000→0, status ISSUED→ISSUED (no payment rows existed — the ₹5,000 "paid" was fabricated)
    68→    INV-1002 (id 2): subtotal 7600→15000, cgst 684→1350, sgst 684→1350, total 8968→17700, amountPaid 3000→0, status PARTIALLY_PAID→ISSUED (no payment rows to justify partial)
    69→    INV-1003 (id 3): subtotal 4200→15000, cgst 378→1350, sgst 378→1350, total 4956→17700, amountPaid 0→0, ISSUED
    70→    INV-1004 (id 4): subtotal 12400→15000, cgst 1116→1350, sgst 1116→1350, total 14632→17700, amountPaid 0→0, DRAFT
    71→    INV-1005 (id 5): subtotal 25500→15000, cgst 2295→1350, sgst 2295→1350, total 30190→17700, amountPaid 15500→500 (= its one real CASH payment row from QA), PARTIALLY_PAID (unchanged)
    72→  Confirmed via API: GET /api/invoices shows subtotal=Σ(lines)=15000 and amountPaid=Σ(payments) for every invoice. Seed change does not require backend restart (seed not loaded by the running server); script left at /home/z/tmp/fix-invoice-totals.js.
    73→- C) Jobcards labeling: AppLayout.jsx nav item /jobcards "Projects"→"Jobcards" (icon kept); JobcardsPage PageHeader "Projects"→"Jobcards", "New Project"→"New Jobcard", error/delete-confirm/toast strings updated ("Project No" column kept as instructed).
    74→- D) Inactive parties: PartiesPage name cell now renders a muted slate "Inactive" Badge + dims the name for isActive=false; added All parties/Active/Inactive select. party.controller.js list has no ?isActive support, so the filter is client-side over the loaded rows (visibleRows) per instructions.
    75→- E) 404: App.jsx catch-all `<Navigate to="/">` replaced by an inline NotFound component (centered card: big "404", "Page not found", explanation, "Back to Dashboard" primary button linking to /); URL no longer silently rewrites.
    76→- Verification: `bun run lint` 0 errors (71 pre-existing warnings, none new); `bunx vite build` OK; dist re-copied to backend/public; `node --check backend/prisma/seed.js` + `npx eslint prisma/seed.js` clean; backend `npm run lint` clean; served HTML references the new bundle and the asset returns 200.
    77→
    78→Stage Summary:
    79→- Commit 4692b66 "fix(ux): amount-in-words on invoices, Jobcards nav label, inactive party badge, 404 page, seed totals reconciliation" on master, parent d050c61. NOTE: the planned HEAD-message amend (typo "security, security") was NOT needed — d050c61's subject already reads exactly "fix: security, stock integrity, UX hardening — full audit remediation" (verified byte-level with od -c), so it was left untouched to avoid a pointless hash rewrite.
    80→- Deployed asset hash: index-MTVP5XtM.js (curl of / shows it; GET /assets/index-MTVP5XtM.js → 200).
    81→- Browser spot-checks (agent-browser, admin@utech.local): /invoices/5 → totals Subtotal ₹15,000.00 / CGST+SGST ₹1,350.00 / Total ₹17,700.00 / Paid ₹500.00 / Balance ₹17,200.00 + "AMOUNT IN WORDS: Seventeen Thousand Seven Hundred Rupees Only"; /jobcards → sidebar item and page heading both "Jobcards"; /parties → "Browser Test Party" row shows "Inactive" badge, filter Inactive shows only P-0011, Active hides it, All shows 11 rows; /some-bogus-url → stays on URL and shows the 404 card, "Back to Dashboard" returns to /. Zero page errors; console only has the benign recharts width/height warnings seen in prior QA.
    82→---
    83→Task ID: 5-a
    84→Agent: preview-restore-agent
    85→Task: Restore ERP preview login + verify credentials
    86→
    87→Work Log:
    88→- Status check on arrival: ports 3000 (next-server pid 1100) + 4000 (backend pid 1167) UP, 3306 DOWN. erp-services.log showed watchdog loop: "starting mariadbd … spawn /home/z/mariadb/root/usr/sbin/mariadbd ENOENT". Backend backend.log confirmed login 500 = Prisma "Can't reach database server at 127.0.0.1:3306".
    89→- ROOT CAUSE: the ENTIRE /home/z/mariadb tree (binaries, libs, AND datadir with all data) had been wiped from disk before this session began (env-level reset — nothing to preserve; task's "never delete datadir" respected: I deleted nothing). Watchdog was healthy, just had no binary to spawn.
    90→- Rebuilt the exact layout instrumentation.ts expects (no Next.js files touched): Debian 13 (trixie) repos carry the identical version 1:11.8.6-0+deb13u1 — `apt-get download mariadb-server-core mariadb-client-core liburing2 libaio1t64 libncurses6 libtinfo6` into /home/z/mariadb-debs, then `dpkg -x` server+client cores into /home/z/mariadb/root/ and the 4 lib debs into /home/z/mariadb/libs/ → /home/z/mariadb/root/usr/sbin/mariadbd, root/usr/bin/{mariadb,mariadb-install-db,…}, libs/usr/lib/x86_64-linux-gnu/{liburing.so.2,libaio.so.1t64,libncurses.so.6,libtinfo.so.6}. Note: Debian ships share files under root/usr/share/mariadb (NOT share/mysql) — mariadbd resolves them fine from --basedir alone, so the watchdog's flag set needs no changes.
    91→- Fresh datadir init (old one unrecoverable): `mariadb-install-db --no-defaults --basedir=/home/z/mariadb/root/usr --datadir=/home/z/mariadb/data --auth-root-authentication-method=normal --skip-test-db` → exit 0. Foreground smoke test with the watchdog's exact flags → "ready for connections" 11.8.6 on 127.0.0.1:3306 + socket /home/z/mariadb/mysqld.sock.
    92→- Started mariadbd, created `CREATE DATABASE IF NOT EXISTS utech; CREATE USER IF NOT EXISTS 'utech'@'%' IDENTIFIED BY 'utech123'; GRANT ALL ON utech.* TO 'utech'@'%'; FLUSH PRIVILEGES;` via root socket auth.
    93→- Exported DATABASE_URL=mysql://utech:utech123@127.0.0.1:3306/utech (sandbox file: override gotcha) → `bunx prisma migrate deploy` (all 29 migrations incl. number_sequence applied) → `node prisma/seed.js` exit 0, idempotent (admin password only set on create; "Done. Login with admin@utech.local / Admin@123").
    94→- API verify: POST /api/auth/login → 200 {token, user SUPERADMIN} through BOTH 127.0.0.1:4000 and 127.0.0.1:3000 (proxy); GET /api/auth/me Bearer → 200 with permissions[]; GET /api/parties → 200 seeded rows (P-0010 Vertex Projects…); GET /health → 200.
    95→- Watchdog handover: `pkill -x mariadbd` + waited 25s → watchdog tick respawnd mariadbd at 15:45:50 as pid 1938 with PPID 1100 (next-server child → survives bash sessions); "ready for connections" in erp-services.log; no spawn errors, no spawn storms; re-verified login 200 after handover.
    96→- Browser verify (agent-browser 0.35.0 headless Chromium): open http://localhost:3000/ → ERP login page (title "UTech Automation ERP", /login after auth-guard redirect); filled admin@utech.local / Admin@123 → redirected to / Dashboard rendering REAL data: Sales(30d) ₹65,254.00 / 4 invoices, Active Jobcards 0, Pending Jobwork 0, Overdue Invoices 0, Total Parties 10, Total Items 10, Sales Performance bar chart (Apr–Sep), Inventory Distribution donut, Production Efficiency empty-state text — no crash boundary; `agent-browser errors` empty. Screenshot: qa-shots/5a-dashboard.png. Also loaded /invoices → all 5 seeded invoices (INV-1001…INV-1005 with PARTIALLY_PAID/DRAFT/ISSUED states).
    97→
    98→Stage Summary:
    99→- Root cause: /home/z/mariadb (MariaDB 11.8.6 user-space install + datadir) had been erased from disk → watchdog spawn ENOENT → 3306 down → Prisma "Can't reach database server" → auth.controller login → 500 {"error":"InternalError"}. Proxy/backend/Next.js were never at fault.
   100→- Fix: rebuilt user-space MariaDB 11.8.6-0+deb13u1 from Debian trixie debs at the exact watchdog paths (root/usr for server+client, libs/usr/lib/x86_64-linux-gnu for liburing2/libaio1t64/libncurses6/libtinfo6), fresh mariadb-install-db init at /home/z/mariadb/data, recreated DB utech + user utech/utech123, prisma migrate deploy (29 migrations) + idempotent seed. Zero changes to Next.js files, backend code, or the watchdog; watchdog then adopted mariadbd as a next-server child (pid 1938, PPID 1100).
   101→- Verified: login 200 via :3000 and :4000; /api/auth/me + /api/parties 200 with data; browser UI login → dashboard with real KPIs/charts; ports 3306/4000/3000 all listening; debs kept at /home/z/mariadb-debs for future re-extraction.
   102→- Caveats: (1) DB content is the fresh seed baseline — QA-era artifacts from previous sessions (party P-0011, ₹500 payment on INV-1005) are gone because the datadir was wiped before this session; fresh seed is reconciled per Task 7 (line-consistent totals, INV-1005 ₹500 CASH payment row). (2) If the platform wipes /home/z/mariadb again, the same ENOENT loop returns — but binaries+datadir now persist on disk, so watchdog auto-revival covers normal sandbox restarts. (3) Watchdog portUp probes still cause harmless MariaDB "Aborted connection" warnings.
   103→---
   104→Task ID: 6-a
   105→Agent: ux-foundation-agent
   106→Task: Shared UX primitives + layout fixes
   107→Work Log:
   108→- Style scan: tailwind.config.js (brand=violet primary, danger=red destructive, slate muted; GOTCHA emerald aliased to violet, cyan to orange), src/styles.css (.btn-*/.input/.label/.card/.badge component classes, global webkit scrollbar, fadeIn/slideUp/scaleIn animations), react-hot-toast, PageHeader/DataTable/Badge/Pagination convention in src/components/ui. Inventoried every form surface via rg over pages+components (see docs/FORMS_UX_PLAN.md §3 for the full table: 56 files, counts of select/input/textarea each).
   109→- Wrote /home/z/my-project/utech-repo/docs/FORMS_UX_PLAN.md — the contract for waves B/C/D: (a) full inventory table with one-line state notes, (b) design-system spec (token mapping + every primitive API + usage patterns), (c) wave assignments (B=sales/masters: quotations, invoices, sales returns, back orders, parties, items; C=ops/purchase/admin: GRN, PO, purchase returns, quality, dispatch, jobcards+components, jobwork, vendor work orders+modals, expenses, users, roles, departments(+subcategories), machines, processes, production+shifts, BOMs, customer material, assignments+AssignModal, projects; D=dashboard/shell/read-only: Dashboard, OperatorDashboard, Login, NotFound, DataTable/Pagination/PageHeader/Badge, Reports, 3 analytics, stock, AppLayout, NotificationBell, ChangePasswordModal), (d) rules for the waves (no new palette, SearchableSelect for entity selects, ConfirmDialog for deletes, EmptyState for empties, actionsBar for form footers).
   110→- Built primitives (plain JSX, zero new deps, app palette only): src/lib/formStyles.js (single `styles` export: input/inputError/textarea/label/hint/errorText/selectBtn/primaryBtn/secondaryBtn/ghostBtn/dangerBtn/formGrid/formGrid3/formGrid4/actionsBar/sectionCard), src/lib/validation.js (required/email/phone10+normalizePhone/gstin/pincode/positiveNumber/gstRate/hsn/passwordStrong + validators map + validateAll(values,schema)→{errors,ok}; validators return error-string|null, format validators allow empty), src/lib/useClickOutside.js (ref+onAway+{active,escape,events}; outside mousedown/touchstart + Escape).
   111→- Built UI components in src/components/ui/: FormField.jsx (label+red required asterisk+hint+error w/ AlertCircle, deterministic ARIA ids `${id}-hint`/`${id}-error`), FormSection.jsx (card-flat section, icon in soft brand circle, title/description/actions slot, p-4 sm:p-5), SearchableSelect.jsx (button-triggered filterable listbox: case-insensitive substring filter over label+subtitle, ArrowUp/Down/Home/End/Enter/Escape/Tab, ARIA listbox/option/aria-expanded/aria-activedescendant, outside-click+scroll-close via capture listener that IGNORES the listbox's own scrolling, Escape in capture phase so it closes only the popover and not an enclosing Modal, clear × when allowClear+value, check icon + active highlight + scrollIntoView, max-h-60 list, loading row, matches-count when >50, trigger shows selected LABEL never raw id, loose String() value comparison, empty error string → '' on clear), Modal.jsx (portal to body, ref-counted body scroll lock for stacked modals, focus trap w/ Tab cycling + initial focus + focus restore, Escape close unless disableEscape, overlay click unless closeOnOverlay, sizes md/lg/xl/full, animate-fade-in overlay + animate-scale-in panel, role=dialog aria-modal aria-labelledby, X button, z-[70]), ConfirmDialog.jsx (on Modal: destructive variant w/ red AlertTriangle + dangerBtn, loading spinner + disabled buttons, Enter=confirm capture listener that skips button/textarea targets, Escape=cancel via Modal), EmptyState.jsx (muted icon in soft circle, title/description, primary sm CTA).
   112→- Layout fixes: AppLayout.jsx user dropdown now closes on outside click AND Escape via new shared useClickOutside hook (was toggle-only). NotificationBell.jsx rebuilt: items are now clickable → routeForNotification(n) maps refType→detail route (VENDOR_WORK_ORDER/:refId, JOBCARD/:refId) then type-prefix fallback (ASSIGNMENT→/assignments, INVOICE→/invoices/:refId, LOW_STOCK→/items, GRN/DISPATCH/returns/PO→section pages), closes popover before navigating, mark-one-read + mark-all-read behavior preserved; popover also closes on Escape now (shared hook). Login/NotFound untouched (wave D).
   113→- Verification: bunx eslint on src/lib + src/components/ui + src/components/layout → 0 problems in new files (only pre-existing Badge.jsx STATUS_ICONS warning); full `bun run lint` → 0 errors, 71 warnings, all pre-existing (matches Task 7's count), none introduced. Sanity compile: per-file esbuild --bundle over all 11 new/changed files → all resolve+transform clean. bunx vite build --outDir /tmp/ux-foundation-build --emptyOutDir → green (2426 modules; only the pre-existing >500kB chunk warning), built CSS contains the new utility classes, temp dir deleted. No form pages modified, nothing deployed, no git commits.
   114→Stage Summary:
   115→- New files: frontend/src/components/ui/{FormField,FormSection,SearchableSelect,Modal,ConfirmDialog,EmptyState}.jsx, frontend/src/lib/{formStyles,validation,useClickOutside}.js, docs/FORMS_UX_PLAN.md. Modified: components/layout/AppLayout.jsx (user menu outside-click/Escape), components/layout/NotificationBell.jsx (clickable notifications + Escape close).
   116→- formStyles contract: `import { styles } from '<rel>/lib/formStyles'` — styles.input (+ styles.inputError concat on error), styles.textarea, styles.label, styles.hint, styles.errorText, styles.selectBtn, styles.primaryBtn/secondaryBtn/ghostBtn/dangerBtn, styles.formGrid/formGrid3/formGrid4 (put FormFields as direct children; full-width field = className "sm:col-span-2"), styles.actionsBar (sticky footer bar), styles.sectionCard.
   117→- Component APIs (all default exports): <FormField id label required hint error className> wraps any control; <FormSection icon title description actions>; <SearchableSelect value onChange(raw value|'') options=[{value,label,subtitle}] placeholder disabled error loading allowClear emptyText className id>; <Modal open onClose title description size='md'|'lg'|'xl'|'full' footer closeOnOverlay disableEscape>; <ConfirmDialog open onClose onConfirm title message confirmLabel cancelLabel variant='default'|'destructive' loading>; <EmptyState icon title description action={{label,onClick,icon}}>; useClickOutside(ref, onAway, {active, escape, events}).
   118→- validation contract: each fn → error-string|null; empty valid for email/phone10/gstin/pincode/gstRate/hsn/passwordStrong; required(v,label) + positiveNumber(v,label,{min,max}) fail empty; validateAll(values,{field:(v,all)=>msg|null}) → {errors,ok}; normalizePhone(v) for payload cleanup.
   119→- Waves B/C/D: read docs/FORMS_UX_PLAN.md BEFORE touching any form — §2 is the API contract, §4 the file assignments, §5 the rules (no new palette; SearchableSelect for entity selects; native <select> only for ≤8 static enums; ConfirmDialog replaces window.confirm; EmptyState for page-level empties). SearchableSelect must keep working inside Modal (Escape layering handled) and inside overflow containers (popover is absolute, not portaled — fine for forms, avoid using it in sticky/transform contexts without testing).
   120→- Known pre-existing lint warnings: 71 (react-hooks/exhaustive-deps on page-level load effects + Badge STATUS_ICONS) — leave alone.